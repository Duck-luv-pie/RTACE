# Copy to .env and set your Grafana credentials if you don't want the defaults (admin/admin).
# Never commit .env - it is in .gitignore.
#
# GRAFANA_ADMIN_USER=your_email@example.com
# GRAFANA_ADMIN_PASSWORD=your_password
